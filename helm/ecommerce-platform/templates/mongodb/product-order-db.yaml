# product-db Headless Service
apiVersion: v1
kind: Service
metadata:
  name: product-db-headless
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: product-db
spec:
  clusterIP: None
  selector:
    {{- include "ecommerce.selectorLabels" (dict "component" "product-db") | nindent 4 }}
  ports:
    - port: 27017
      targetPort: 27017
      name: mongo
---
# product-db ClusterIP Service
apiVersion: v1
kind: Service
metadata:
  name: product-db
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: product-db
spec:
  type: ClusterIP
  selector:
    {{- include "ecommerce.selectorLabels" (dict "component" "product-db") | nindent 4 }}
  ports:
    - port: 27017
      targetPort: 27017
      name: mongo
---
# product-db StatefulSet
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: product-db
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: product-db
spec:
  serviceName: product-db-headless
  replicas: 1
  selector:
    matchLabels:
      {{- include "ecommerce.selectorLabels" (dict "component" "product-db") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ecommerce.selectorLabels" (dict "component" "product-db") | nindent 8 }}
        {{- include "ecommerce.labels" . | nindent 8 }}
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        fsGroup: 999
      containers:
        - name: mongodb
          image: {{ printf "%s:%s" .Values.mongodb.image.repository .Values.mongodb.image.tag | quote }}
          imagePullPolicy: {{ .Values.mongodb.image.pullPolicy }}
          ports:
            - containerPort: 27017
              name: mongo
          securityContext:
            runAsNonRoot: true
            runAsUser: 999
            runAsGroup: 999
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            {{- toYaml .Values.mongodb.resources | nindent 12 }}
          volumeMounts:
            - name: product-db-data
              mountPath: /data/db
          readinessProbe:
            exec:
              command: ["mongosh", "--eval", "db.adminCommand('ping')"]
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            exec:
              command: ["mongosh", "--eval", "db.adminCommand('ping')"]
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
  volumeClaimTemplates:
    - metadata:
        name: product-db-data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: {{ .Values.global.storageClass | quote }}
        resources:
          requests:
            storage: {{ .Values.mongodb.storage.productDb.size | quote }}
---
# order-db Headless Service
apiVersion: v1
kind: Service
metadata:
  name: order-db-headless
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: order-db
spec:
  clusterIP: None
  selector:
    {{- include "ecommerce.selectorLabels" (dict "component" "order-db") | nindent 4 }}
  ports:
    - port: 27017
      targetPort: 27017
      name: mongo
---
# order-db ClusterIP Service
apiVersion: v1
kind: Service
metadata:
  name: order-db
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: order-db
spec:
  type: ClusterIP
  selector:
    {{- include "ecommerce.selectorLabels" (dict "component" "order-db") | nindent 4 }}
  ports:
    - port: 27017
      targetPort: 27017
      name: mongo
---
# order-db StatefulSet
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: order-db
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/component: order-db
spec:
  serviceName: order-db-headless
  replicas: 1
  selector:
    matchLabels:
      {{- include "ecommerce.selectorLabels" (dict "component" "order-db") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ecommerce.selectorLabels" (dict "component" "order-db") | nindent 8 }}
        {{- include "ecommerce.labels" . | nindent 8 }}
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        fsGroup: 999
      containers:
        - name: mongodb
          image: {{ printf "%s:%s" .Values.mongodb.image.repository .Values.mongodb.image.tag | quote }}
          imagePullPolicy: {{ .Values.mongodb.image.pullPolicy }}
          ports:
            - containerPort: 27017
              name: mongo
          securityContext:
            runAsNonRoot: true
            runAsUser: 999
            runAsGroup: 999
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            {{- toYaml .Values.mongodb.resources | nindent 12 }}
          volumeMounts:
            - name: order-db-data
              mountPath: /data/db
          readinessProbe:
            exec:
              command: ["mongosh", "--eval", "db.adminCommand('ping')"]
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            exec:
              command: ["mongosh", "--eval", "db.adminCommand('ping')"]
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
  volumeClaimTemplates:
    - metadata:
        name: order-db-data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: {{ .Values.global.storageClass | quote }}
        resources:
          requests:
            storage: {{ .Values.mongodb.storage.orderDb.size | quote }}
