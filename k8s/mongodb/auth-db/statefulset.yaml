# =============================================================================
# auth-db — Headless Service (for StatefulSet stable network identity)
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: auth-db-headless
  namespace: ecommerce-dev
  labels:
    app.kubernetes.io/name: shopnova
    app.kubernetes.io/component: auth-db
spec:
  clusterIP: None
  selector:
    app: auth-db
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
      name: mongo
---
# =============================================================================
# auth-db — ClusterIP Service (for backend service connections)
# DNS: auth-db.ecommerce-dev.svc.cluster.local:27017
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: auth-db
  namespace: ecommerce-dev
  labels:
    app.kubernetes.io/name: shopnova
    app.kubernetes.io/component: auth-db
spec:
  type: ClusterIP
  selector:
    app: auth-db
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
      name: mongo
---
# =============================================================================
# auth-db — StatefulSet
# =============================================================================
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: auth-db
  namespace: ecommerce-dev
  labels:
    app.kubernetes.io/name: shopnova
    app.kubernetes.io/component: auth-db
spec:
  serviceName: auth-db-headless
  replicas: 1
  selector:
    matchLabels:
      app: auth-db
  template:
    metadata:
      labels:
        app: auth-db
        app.kubernetes.io/name: shopnova
        app.kubernetes.io/component: auth-db
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        fsGroup: 999
      containers:
        - name: mongodb
          image: mongo:7
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 27017
              name: mongo
          securityContext:
            runAsNonRoot: true
            runAsUser: 999
            runAsGroup: 999
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: auth-db-data
              mountPath: /data/db
          readinessProbe:
            exec:
              command:
                - mongosh
                - "--eval"
                - "db.adminCommand('ping')"
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            exec:
              command:
                - mongosh
                - "--eval"
                - "db.adminCommand('ping')"
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
  volumeClaimTemplates:
    - metadata:
        name: auth-db-data
        labels:
          app.kubernetes.io/name: shopnova
          app.kubernetes.io/component: auth-db
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: standard
        resources:
          requests:
            storage: 1Gi
